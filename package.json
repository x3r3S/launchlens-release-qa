{
  "name": "launchlens-release-qa",
  "private": true,
  "version": "1.2.0",
  "description": "A reproducible release-QA evidence workspace with a fixed-build retest.",
  "license": "SEE LICENSE IN PORTFOLIO-REVIEW-LICENSE.md",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "demo": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/domain.mjs && node --check src/app.mjs && node --check scripts/serve.mjs && node --check scripts/verify-proof.mjs",
    "verify:evidence": "node scripts/verify-proof.mjs",
    "verify": "npm run check && npm test && npm run verify:evidence"
  }
}
